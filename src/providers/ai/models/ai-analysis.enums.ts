export enum AiRecommendation {
  QUALIFIED = 'QUALIFIED',
  WAIT = 'WAIT',
  REJECT = 'REJECT',
}

export enum RiskLevel {
  UNKNOWN = 'UNKNOWN',
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
  CRITICAL = 'CRITICAL',
}
